# Operator

The operator view is for attendants and store staff. It is local/internal only and should expose POS tasks and plain-language help, not package controls.

Attendants can use the home page and Help Center. Package installation,
package disable/uninstall, agent changes, exports, publishing, and approval
workflows require owner/admin access.
